torch==2.10.0
torchaudio==2.10.0
torchvision==0.25.0
transformers>=4.57.6
tiktoken>=0.7.0
peft==0.6.0
vllm==0.19.1
trl>=0.15.0
datasets>=2.14.0
accelerate>=0.25.0
# Qwen3.5 GatedDeltaNet kernels (required for fast linear-attention training).
# causal-conv1d + flash-linear-attention provide the optimized fwd/bwd; without
# them transformers falls back to pure-PyTorch and training is ~5-10× slower.
# tilelang is required as an alternative backend on Hopper GPUs because
# Triton ≥3.4 has a backward correctness bug in fla's chunk_bwd_dqkwg (see
# fla-org/flash-linear-attention#640). Install with CUDA module loaded and
# --no-build-isolation for causal-conv1d.
causal-conv1d==1.6.2.post1
flash-linear-attention==0.5.0
tilelang==0.1.10
langchain>=0.3.5
langchain-community>=0.3.5
langchain-openai>=0.3.5
langgraph>=0.2.0
e2b-code-interpreter>=2.0.0
